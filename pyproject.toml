[project]
name = "xpipe-to-ssh"
version = "1.0.0"
description = "Convert an in XPipe configured connection to a SSH command."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "rich>=15.0.0",
    "xpipe-api>=0.1.33",
]
